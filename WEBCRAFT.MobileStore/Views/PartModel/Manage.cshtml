@model PartModelViewModel
@{
    ViewBag.Title = "Manage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Mode</h2>

@using (Html.BeginForm("Manage", "Subcategory", FormMethod.Post))
{
    <div>
        <div class="form-group">
            @Html.LabelFor(p => p.ModelName)
            @Html.TextBoxFor(b => b.Subcategory.Name, new { @Class = "form-control" })
        </div>

        <div class="form-group">
            @Html.DropDownListFor(b => b.Subcategory.Fk_BrandId, new SelectList(Model.Brands, "Id", "Name" ), "Select Category" ,new {@class = "form-control" })
        </div>

        @Html.HiddenFor(b => b.Subcategory.Id)

        <div class="form-group">
            <input type="submit" value="@ViewBag.Mode" class="btn btn-primary" />
        </div>
    </div>
}
