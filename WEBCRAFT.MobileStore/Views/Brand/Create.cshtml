@model CategoryVeiwModels
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Mode</h2>

@using (Html.BeginForm("Manage", "Brand", FormMethod.Post))
{
    <div>
        <div class="form-group">
            @Html.LabelFor(b => b.CategoryName)
            @Html.TextBoxFor(b => b.Category.Name, new { @Class = "form-control" })
        </div>
        @Html.HiddenFor(b => b.Category.Id)

        <div class="form-group">
            <input type="submit" value="@ViewBag.Mode" class="btn btn-primary" />
        </div>
    </div>
}